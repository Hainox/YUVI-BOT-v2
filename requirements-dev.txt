# Dev/test-зависимости — НЕ ставятся в прод-образ (bot.Dockerfile использует только requirements.txt).
# Версии зафиксированы точным == после сверки с PyPI (см. checkpoint-задачу 01-01 Task 2).
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
