.venv/
.venv*/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.idea/
.vscode/
node_modules/
dist/
build/
.DS_Store
.env
*.log
coverage.xml
htmlcov/

# Generated benchmark output (scripts/q_bench.py)
scripts/q_bench_results.json

# GSD planning docs — local-only, contains private reference-repo analysis
.planning/

# Freebuff desktop app local state (thread DBs, project ids) — tool metadata, not source
.freebuff/

# Local dev-only image for running DB-touching commands inside the docker
# network on Windows hosts where Docker Desktop's host<->container asyncpg
# connections are unreliable (see 01-01-SUMMARY.md "Issues Encountered")
Dockerfile.dev

# Kurigram/Pyrogram MTProto session files — these are live login credentials
# for the personal Telegram account used for backfill (DATA-04), equivalent
# to a password. Never commit.
*.session
*.session-journal

# Character concept-art reference folders (Telegram photo exports) — local
# design reference only, not project source.
АМИРА - Концепты/
АСТРЕЯ КОНЦЕПТЫ/
Дизайн бота Yuvi с гачей системой (1)/
МАРА - КОНЦЕПТЫ/
СОФИЯ - КОНЦЕПТЫ/
ЭЛИАНА _ КОНЦЕПТЫ/
ЮНА КОНЦЕПТЫ/

backups/
